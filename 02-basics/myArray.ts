const superheros: string[] = []
const heroIds: Array<number> = []

type User = {
    name: string,
    isActive: boolean
}

const allUsers: User[] = []

const MLModels: number[][] = [
    [255,255,255], [123131]
]

superheros.push('hola')
heroIds.push(2)

allUsers.push({name: 'josep', isActive: true})

export {}